export function PortalHeader({ eyebrow, title, description }: { eyebrow: string; title: string; description?: string }) { return <header><p className="text-xs uppercase tracking-[.22em] text-gold">{eyebrow}</p><h1 className="mt-2 font-display text-5xl">{title}</h1>{description && <p className="mt-3 max-w-2xl text-sm leading-6 text-black/50">{description}</p>}</header>; }
export function EmptyPanel({ title, message }: { title: string; message: string }) { return <section className="mt-8 border border-black/10 bg-paper p-8"><h2 className="font-display text-2xl">{title}</h2><p className="mt-2 text-sm leading-6 text-black/50">{message}</p></section>; }
