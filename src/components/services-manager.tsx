"use client";import Image from"next/image";import{Eye,Pencil,Plus,Trash2,X}from"lucide-react";import{useState}from"react";import{ActionForm}from"@/components/action-form";import{SaveButton,TextArea,TextField}from"@/components/admin-form-fields";import{deleteService,saveService}from"@/app/admin/services/actions";
type Service={id:string;name:string;slug:string;description:string|null;image_url:string|null;price_minor:number;pricing_type:string;duration_minutes:number|null;sort_order:number;active:boolean;featured:boolean};
export function ServicesManager({items}:{items:Service[]}){const[editing,setEditing]=useState<Service|null|"new">(null),[viewing,setViewing]=useState<Service|null>(null);return <><div className="mt-8 flex justify-end"><button onClick={()=>setEditing("new")} className="inline-flex items-center gap-2 bg-ink px-4 py-3 text-sm text-paper transition hover:bg-gold hover:text-ink"><Plus size={17}/> Add service</button></div><div className="mt-4 overflow-x-auto border border-black/10 bg-paper"><table className="w-full min-w-[46rem] text-left text-sm"><thead className="border-b border-black/10 text-black/45"><tr><th className="p-4">Service</th><th className="p-4">Price</th><th className="p-4">Status</th><th className="p-4 text-right">Actions</th></tr></thead><tbody>{items.map(item=><tr key={item.id} className="border-b border-black/10 transition hover:bg-white"><td className="p-4"><strong>{item.name}</strong><small className="block text-black/45">/{item.slug}</small></td><td className="p-4">GHS {(item.price_minor/100).toFixed(2)} · {item.pricing_type}</td><td className="p-4">{item.active?"Active":"Hidden"}</td><td className="p-4"><div className="flex justify-end gap-2"><Icon label="View" onClick={()=>setViewing(item)}><Eye size={16}/></Icon><Icon label="Edit" onClick={()=>setEditing(item)}><Pencil size={16}/></Icon><ActionForm action={deleteService} successMessage={`${item.name} deleted.`} className="inline"><input type="hidden" name="id" value={item.id}/><button aria-label={`Delete ${item.name}`} className="grid size-9 place-items-center border border-black/15 text-red-700 transition hover:bg-red-700 hover:text-white"><Trash2 size={16}/></button></ActionForm></div></td></tr>)}</tbody></table></div>{editing&&<Modal title={editing==="new"?"Add service":`Edit ${editing.name}`} close={()=>setEditing(null)}><ServiceForm item={editing==="new"?undefined:editing}/></Modal>}{viewing&&<Modal title={viewing.name} close={()=>setViewing(null)}><div className="space-y-4">{viewing.image_url&&<div className="relative aspect-video overflow-hidden bg-black/5"><Image src={viewing.image_url} alt={viewing.name} fill className="object-cover" unoptimized/></div>}<p className="leading-7 text-black/60">{viewing.description||"No description."}</p><p><strong>GHS {(viewing.price_minor/100).toFixed(2)}</strong> · {viewing.pricing_type}</p></div></Modal>}</>}
function ServiceForm({item}:{item?:Service}){return <ActionForm action={saveService} successMessage={item?"Service updated.":"Service created."} className="grid gap-4"><input type="hidden" name="id" value={item?.id||""}/><input type="hidden" name="current_image_url" value={item?.image_url||""}/><div className="grid gap-4 sm:grid-cols-2"><TextField name="name" label="Name" value={item?.name} required/><TextField name="slug" label="Slug" value={item?.slug} required/></div><TextArea name="description" label="Description" value={item?.description}/><label className="text-sm"><span className="mb-2 block">Service image</span><input name="image" type="file" accept="image/jpeg,image/png,image/webp,image/gif" className="w-full border border-black/15 bg-white p-3"/></label><div className="grid gap-4 sm:grid-cols-2"><TextField name="price" label="Price (GHS)" type="number" value={item?item.price_minor/100:0}/><label className="text-sm"><span className="mb-2 block">Pricing</span><select name="pricing_type" defaultValue={item?.pricing_type||"fixed"} className="w-full border border-black/15 bg-white p-3">{["hourly","daily","fixed","percentage","custom"].map(x=><option key={x}>{x}</option>)}</select></label><TextField name="duration_minutes" label="Duration (minutes)" type="number" value={item?.duration_minutes}/><TextField name="sort_order" label="Sort order" type="number" value={item?.sort_order||0}/></div><div className="flex gap-5 text-sm"><label><input type="checkbox" name="active" defaultChecked={item?.active??true} className="mr-2"/>Active</label><label><input type="checkbox" name="featured" defaultChecked={item?.featured} className="mr-2"/>Featured</label></div><SaveButton label={item?"Save changes":"Create service"}/></ActionForm>}
function Modal({title,close,children}:{title:string;close:()=>void;children:React.ReactNode}){return <div className="fixed inset-0 z-[60] grid place-items-center overflow-y-auto bg-black/55 p-4"><section role="dialog" aria-modal="true" className="my-8 w-full max-w-2xl bg-paper p-6 shadow-2xl"><div className="mb-6 flex items-center justify-between"><h2 className="font-display text-3xl">{title}</h2><button onClick={close} aria-label="Close"><X/></button></div>{children}</section></div>}
function Icon({label,onClick,children}:{label:string;onClick:()=>void;children:React.ReactNode}){return <button aria-label={label} onClick={onClick} className="grid size-9 place-items-center border border-black/15 transition hover:border-gold hover:bg-gold">{children}</button>}
