import Link from "next/link";
import { logout } from "@/app/(auth)/actions";
export function PortalShell({ title, nav, children }: { title: string; nav: { href: string; label: string }[]; children: React.ReactNode }) { return <div className="min-h-screen bg-[#ebe7de]"><header className="border-b border-black/10 bg-paper"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5"><Link href="/" className="font-display text-xl tracking-[.18em]">STUDIO TWO</Link><form action={logout}><button className="text-sm text-black/60">Sign out</button></form></div></header><div className="mx-auto grid max-w-7xl gap-8 px-6 py-10 md:grid-cols-[13rem_1fr]"><aside><p className="mb-6 text-xs uppercase tracking-[.22em] text-black/45">{title}</p><nav className="space-y-1">{nav.map(item => <Link key={item.href} href={item.href} className="block border-l border-black/15 px-4 py-2.5 text-sm hover:border-gold hover:bg-white/50">{item.label}</Link>)}</nav></aside><main>{children}</main></div></div>; }
