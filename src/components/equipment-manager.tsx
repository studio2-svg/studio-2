"use client";import Image from"next/image";import{Eye,Pencil,Plus,Trash2,X}from"lucide-react";import{useState}from"react";import{ActionForm}from"@/components/action-form";import{SaveButton,TextArea,TextField}from"@/components/admin-form-fields";import{deleteEquipment,saveEquipment}from"@/app/admin/equipment/actions";
type Category={id:string;name:string};type Item={id:string;category_id:string|null;name:string;slug:string;description:string|null;image_url:string|null;total_quantity:number;price_minor:number;pricing_type:string;status:string;featured:boolean;equipment_categories:{name:string}|null};
export function EquipmentManager({items,categories}:{items:Item[];categories:Category[]}){const[editing,setEditing]=useState<Item|null|"new">(null),[viewing,setViewing]=useState<Item|null>(null);return <><div className="mt-8 flex justify-end"><button onClick={()=>setEditing("new")} className="inline-flex items-center gap-2 bg-ink px-4 py-3 text-sm text-paper transition hover:bg-gold hover:text-ink"><Plus size={17}/> Add equipment</button></div><div className="mt-4 overflow-x-auto border border-black/10 bg-paper"><table className="w-full min-w-[50rem] text-left text-sm"><thead className="border-b border-black/10 text-black/45"><tr><th className="p-4">Equipment</th><th className="p-4">Category</th><th className="p-4">Quantity</th><th className="p-4">Price</th><th className="p-4">Status</th><th className="p-4 text-right">Actions</th></tr></thead><tbody>{items.map(item=><tr key={item.id} className="border-b border-black/10 transition hover:bg-white"><td className="p-4 font-medium">{item.name}</td><td className="p-4">{item.equipment_categories?.name||"—"}</td><td className="p-4">{item.total_quantity}</td><td className="p-4">GHS {(item.price_minor/100).toFixed(2)}</td><td className="p-4">{item.status}</td><td className="p-4"><div className="flex justify-end gap-2"><Icon onClick={()=>setViewing(item)}><Eye size={16}/></Icon><Icon onClick={()=>setEditing(item)}><Pencil size={16}/></Icon><ActionForm action={deleteEquipment} successMessage={`${item.name} deleted.`} className="inline"><input type="hidden" name="id" value={item.id}/><button aria-label="Delete" className="grid size-9 place-items-center border border-black/15 text-red-700 transition hover:bg-red-700 hover:text-white"><Trash2 size={16}/></button></ActionForm></div></td></tr>)}</tbody></table></div>{editing&&<Modal title={editing==="new"?"Add equipment":`Edit ${editing.name}`} close={()=>setEditing(null)}><Form item={editing==="new"?undefined:editing} categories={categories}/></Modal>}{viewing&&<Modal title={viewing.name} close={()=>setViewing(null)}>{viewing.image_url&&<div className="relative aspect-video"><Image src={viewing.image_url} alt={viewing.name} fill className="object-cover" unoptimized/></div>}<p className="mt-4 text-black/60">{viewing.description||"No description."}</p></Modal>}</>}
function Form({item,categories}:{item?:Item;categories:Category[]}){return <ActionForm action={saveEquipment} successMessage={item?"Equipment updated.":"Equipment created."} className="grid gap-4"><input type="hidden" name="id" value={item?.id||""}/><input type="hidden" name="current_image_url" value={item?.image_url||""}/><div className="grid gap-4 sm:grid-cols-2"><TextField name="name" label="Name" value={item?.name} required/><TextField name="slug" label="Slug" value={item?.slug} required/></div><TextArea name="description" label="Description" value={item?.description}/><label className="text-sm"><span className="mb-2 block">Equipment image</span><input name="image" type="file" accept="image/jpeg,image/png,image/webp,image/gif" className="w-full border border-black/15 bg-white p-3"/></label><div className="grid gap-4 sm:grid-cols-3"><label className="text-sm"><span className="mb-2 block">Category</span><select name="category_id" defaultValue={item?.category_id||""} className="w-full border border-black/15 bg-white p-3"><option value="">Uncategorised</option>{categories.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><TextField name="total_quantity" label="Quantity" type="number" value={item?.total_quantity??1}/><TextField name="price" label="Price (GHS)" type="number" value={item?item.price_minor/100:0}/></div><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm"><span className="mb-2 block">Pricing</span><select name="pricing_type" defaultValue={item?.pricing_type||"fixed"} className="w-full border border-black/15 bg-white p-3">{["hourly","daily","fixed","included"].map(x=><option key={x}>{x}</option>)}</select></label><label className="text-sm"><span className="mb-2 block">Status</span><select name="status" defaultValue={item?.status||"available"} className="w-full border border-black/15 bg-white p-3">{["available","unavailable","maintenance","retired"].map(x=><option key={x}>{x}</option>)}</select></label></div><label className="text-sm"><input type="checkbox" name="featured" defaultChecked={item?.featured} className="mr-2"/>Featured</label><SaveButton label={item?"Save changes":"Create equipment"}/></ActionForm>}
function Modal({title,close,children}:{title:string;close:()=>void;children:React.ReactNode}){return <div className="fixed inset-0 z-[60] grid place-items-center overflow-y-auto bg-black/55 p-4"><section role="dialog" aria-modal="true" className="my-8 w-full max-w-2xl bg-paper p-6"><div className="mb-6 flex justify-between"><h2 className="font-display text-3xl">{title}</h2><button onClick={close}><X/></button></div>{children}</section></div>}
function Icon({onClick,children}:{onClick:()=>void;children:React.ReactNode}){return <button onClick={onClick} className="grid size-9 place-items-center border border-black/15 transition hover:bg-gold">{children}</button>}
