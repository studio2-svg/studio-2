type Initialize={status:boolean;message:string;data?:{authorization_url:string;reference:string}};
export function paystackSecret(){const key=process.env.PAYSTACK_SECRET_KEY;if(!key)throw new Error("Online payment is being configured. Please contact Studio Two.");return key}
export async function initializePaystack(input:{email:string;amount:number;reference:string;callbackUrl:string;metadata:Record<string,unknown>}){const response=await fetch("https://api.paystack.co/transaction/initialize",{method:"POST",headers:{Authorization:`Bearer ${paystackSecret()}`,"Content-Type":"application/json"},body:JSON.stringify({email:input.email,amount:String(input.amount),currency:"GHS",channels:["card","mobile_money"],reference:input.reference,callback_url:input.callbackUrl,metadata:JSON.stringify(input.metadata)})});const result=(await response.json()) as Initialize;if(!response.ok||!result.status||!result.data)throw new Error(result.message||"Payment checkout could not be started.");return result.data}
export async function verifyPaystack(reference:string){const response=await fetch(`https://api.paystack.co/transaction/verify/${encodeURIComponent(reference)}`,{headers:{Authorization:`Bearer ${paystackSecret()}`},cache:"no-store"});const result=await response.json() as {status:boolean;data?:{status:string;amount:number;currency:string;paid_at:string;metadata:Record<string,unknown>}};if(!response.ok||!result.status||!result.data)throw new Error("Payment could not be verified.");return result.data}
