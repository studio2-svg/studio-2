/* eslint-disable @next/next/no-img-element -- Homepage media is selected from the managed CMS. */
import Link from"next/link";import{ArrowUpRight}from"lucide-react";import type{Metadata}from"next";import{PublicNav}from"@/components/public-nav";import{PublicFooter}from"@/components/public-footer";import{getPublishedPage,getPublicCollection,getPublicData,getPublicEquipment,getPublicStaff,pageMetadata}from"@/lib/cms";
type Service={id:string;name:string;description:string|null;price_minor:number;pricing_type:string;featured:boolean};type Equipment={id:string;name:string;description:string|null;image_url:string|null;equipment_categories:{name:string}|null};type Staff={id:string;name:string;role_title:string|null;profile_photo_url:string|null;staff_categories:{name:string}|null};type Gallery={id:string;title:string;media_url:string;media_type:string;category:string};type Testimonial={id:string;customer_name:string;review:string;company:string|null};type Faq={id:string;question:string;answer:string};
export async function generateMetadata():Promise<Metadata>{return pageMetadata(await getPublishedPage('home'))}
export default async function Home(){const[page,services,equipment,team,gallery,testimonials,faqs]=await Promise.all([getPublishedPage('home'),getPublicData<Service>('services'),getPublicEquipment<Equipment>(),getPublicStaff<Staff>(),getPublicCollection<Gallery>('gallery_items'),getPublicCollection<Testimonial>('testimonials'),getPublicCollection<Faq>('faqs')]);if(!page)return <main className="flex min-h-screen items-center justify-center bg-ink px-6 text-paper"><p>Connect Supabase and apply migrations to load published content.</p></main>;const cta=page.content.primary_cta as{label?:string;url?:string}|undefined;const copy=(page.content.home_sections||{})as Record<string,string>;return <main className="bg-paper text-ink"><div className="bg-ink text-paper"><PublicNav dark/><section className="mx-auto grid min-h-[78vh] max-w-7xl content-end px-6 pb-16 lg:grid-cols-[1fr_22rem] lg:gap-20 lg:px-10 lg:pb-24"><div><p className="mb-7 text-xs uppercase tracking-[.32em] text-gold">{page.subtitle}</p><h1 className="max-w-4xl font-display text-6xl leading-[.88] sm:text-8xl lg:text-[8.5rem]">{page.title}</h1></div><div className="mt-10 self-end border-t border-white/20 pt-6"><p className="leading-7 text-white/65">{page.description}</p><Link href={cta?.url||'/book'} className="mt-8 inline-flex items-center gap-2 text-sm uppercase tracking-[.2em] text-gold">{cta?.label||'Start a booking'} <ArrowUpRight size={17}/></Link></div></section></div>
<section className="mx-auto grid max-w-7xl gap-12 px-6 py-24 lg:grid-cols-[1fr_1.3fr]"><p className="text-xs uppercase tracking-[.25em] text-gold">{copy.intro_eyebrow}</p><div><h2 className="font-display text-5xl leading-tight sm:text-6xl">{copy.intro_heading}</h2><p className="mt-6 max-w-2xl text-lg leading-8 text-black/55">{copy.intro_description}</p><Link href="/studio" className="mt-8 inline-flex items-center gap-2 text-sm uppercase tracking-[.18em]">Explore the studio <ArrowUpRight size={16}/></Link></div></section>
<section className="bg-[#e7e1d6] px-6 py-24"><div className="mx-auto max-w-7xl"><Heading eyebrow="Production services" title="Support at every stage." href="/services"/><div className="mt-12 grid gap-px bg-black/10 md:grid-cols-3">{services.slice(0,6).map(item=><article key={item.id} className="bg-[#e7e1d6] p-7"><p className="text-xs uppercase tracking-[.18em] text-gold">{item.pricing_type}</p><h3 className="mt-3 font-display text-3xl">{item.name}</h3><p className="mt-4 text-sm leading-6 text-black/50">{item.description}</p></article>)}</div></div></section>
<section className="mx-auto max-w-7xl px-6 py-24"><Heading eyebrow="Production equipment" title="Tools chosen for the work." href="/equipment"/><div className="mt-12 grid gap-6 md:grid-cols-3">{equipment.slice(0,3).map(item=><article key={item.id}>{item.image_url?<div className="aspect-[4/3] overflow-hidden bg-black/5"><img src={item.image_url} alt={item.name} className="h-full w-full object-cover"/></div>:<div className="aspect-[4/3] bg-ink"/>}<p className="mt-4 text-xs uppercase tracking-[.18em] text-gold">{item.equipment_categories?.name||'Equipment'}</p><h3 className="mt-2 font-display text-3xl">{item.name}</h3></article>)}</div></section>
<section className="bg-ink px-6 py-24 text-paper"><div className="mx-auto max-w-7xl"><Heading eyebrow="The production team" title="Specialists, when you need them." href="/team" dark/><div className="mt-12 grid gap-8 md:grid-cols-3">{team.slice(0,3).map(person=><article key={person.id}>{person.profile_photo_url?<div className="aspect-[4/5] overflow-hidden bg-white/5"><img src={person.profile_photo_url} alt={person.name} className="h-full w-full object-cover"/></div>:<div className="aspect-[4/5] bg-white/5"/>}<p className="mt-4 text-xs uppercase tracking-[.18em] text-gold">{person.staff_categories?.name||person.role_title}</p><h3 className="mt-2 font-display text-3xl">{person.name}</h3></article>)}</div></div></section>
{gallery.length>0&&<section className="px-6 py-24"><div className="mx-auto max-w-7xl"><Heading eyebrow="Selected work" title="Made at Studio Two." href="/gallery"/><div className="mt-12 grid gap-5 md:grid-cols-2">{gallery.slice(0,4).map(item=><article key={item.id} className="relative aspect-[4/3] overflow-hidden bg-black/5">{item.media_type==='image'&&<img src={item.media_url} alt={item.title} className="h-full w-full object-cover"/>}<span className="absolute bottom-4 left-4 bg-ink px-3 py-2 text-xs uppercase tracking-[.15em] text-paper">{item.category}</span></article>)}</div></div></section>}
{testimonials.length>0&&<section className="bg-[#e7e1d6] px-6 py-24"><div className="mx-auto max-w-5xl text-center"><p className="text-xs uppercase tracking-[.25em] text-gold">Client stories</p><blockquote className="mt-8 font-display text-4xl leading-tight sm:text-6xl">“{testimonials[0].review}”</blockquote><p className="mt-7 text-sm">{testimonials[0].customer_name}{testimonials[0].company&&` · ${testimonials[0].company}`}</p></div></section>}
<section className="mx-auto grid max-w-7xl gap-12 px-6 py-24 lg:grid-cols-2"><div><p className="text-xs uppercase tracking-[.25em] text-gold">Before you book</p><h2 className="mt-4 font-display text-6xl">Good questions deserve clear answers.</h2><Link href="/faq" className="mt-8 inline-block text-sm uppercase tracking-[.18em]">See all FAQs →</Link></div><div className="divide-y divide-black/15">{faqs.slice(0,4).map(item=><details key={item.id} className="py-5"><summary className="cursor-pointer list-none font-medium">{item.question}</summary><p className="mt-4 text-sm leading-6 text-black/55">{item.answer}</p></details>)}</div></section>
<section className="bg-gold px-6 py-24 text-ink"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-8 md:flex-row md:items-end"><h2 className="max-w-3xl font-display text-6xl leading-none sm:text-7xl">Bring the idea. We’ll help build the production.</h2><Link href="/book" className="inline-flex items-center gap-2 border border-ink px-6 py-4 text-sm uppercase tracking-[.18em]">Book Studio Two <ArrowUpRight size={17}/></Link></div></section><PublicFooter/></main>}
function Heading({eyebrow,title,href,dark=false}:{eyebrow:string;title:string;href:string;dark?:boolean}){return <div className="flex items-end justify-between gap-5"><div><p className="text-xs uppercase tracking-[.25em] text-gold">{eyebrow}</p><h2 className="mt-4 font-display text-5xl sm:text-6xl">{title}</h2></div><Link href={href} className={`hidden text-sm uppercase tracking-[.15em] sm:block ${dark?'text-white/60':'text-black/55'}`}>View all →</Link></div>}
