import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import type { Metadata } from "next";
import { getPublishedPage, pageMetadata } from "@/lib/cms";
export async function generateMetadata(): Promise<Metadata> { return pageMetadata(await getPublishedPage("home")); }
export default async function Home() { const page = await getPublishedPage("home"); if (!page) return <main className="flex min-h-screen items-center justify-center bg-ink px-6 text-paper"><p>Connect Supabase and apply migrations to load published content.</p></main>; const cta = page.content.primary_cta as { label?: string; url?: string } | undefined; return <main className="min-h-screen bg-ink text-paper"><nav className="mx-auto flex max-w-7xl items-center justify-between px-6 py-7 lg:px-10"><Link href="/" className="font-display text-2xl font-semibold tracking-[.18em]">STUDIO TWO</Link><div className="flex items-center gap-6 text-sm"><Link href="/about">About</Link><Link href="/login">Sign in</Link><Link href="/book" className="border border-gold px-5 py-3 text-gold">Book the studio</Link></div></nav><section className="mx-auto grid min-h-[78vh] max-w-7xl content-end px-6 pb-16 lg:grid-cols-[1fr_22rem] lg:gap-20 lg:px-10 lg:pb-24"><div><p className="mb-7 text-xs uppercase tracking-[.32em] text-gold">{page.subtitle}</p><h1 className="max-w-4xl font-display text-6xl leading-[.88] sm:text-8xl lg:text-[8.5rem]">{page.title}</h1></div><div className="mt-10 self-end border-t border-white/20 pt-6"><p className="leading-7 text-white/65">{page.description}</p><Link href={cta?.url || "/book"} className="mt-8 inline-flex items-center gap-2 text-sm uppercase tracking-[.2em] text-gold">{cta?.label || "Start a booking"} <ArrowUpRight size={17}/></Link></div></section></main>; }
