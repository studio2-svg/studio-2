import {requireAdmin} from "@/lib/auth";import {saveFooter} from "../content-actions";import {CollectionHeader,SaveButton,StatusField,TextArea,TextField} from "@/components/admin-form-fields";
type Footer={description?:string;address?:string;phone?:string;email?:string;opening_hours?:string;copyright?:string};
export default async function FooterAdmin(){const{supabase}=await requireAdmin();const{data,error}=await supabase.from("site_settings").select("value,status").eq("key","footer").maybeSingle();if(error)throw new Error(error.message);const value=(data?.value||{}) as Footer;return <><CollectionHeader eyebrow="Website" title="Footer" description="Manage contact details and business information without code changes."/><form action={saveFooter} className="mt-8 grid gap-4 bg-paper p-6"><TextArea name="description" label="Studio description" value={value.description}/><TextArea name="address" label="Address" value={value.address}/><div className="grid gap-4 sm:grid-cols-2"><TextField name="phone" label="Phone" value={value.phone}/><TextField name="email" label="Email" type="email" value={value.email}/></div><TextArea name="opening_hours" label="Opening hours" value={value.opening_hours}/><TextField name="copyright" label="Copyright text" value={value.copyright}/><StatusField value={data?.status}/><SaveButton/></form></>}
