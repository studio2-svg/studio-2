import { requireAdmin } from "@/lib/auth";import {PortalHeader} from "@/components/portal-page";
export default async function Page(){const{supabase}=await requireAdmin();const[countCustomers,countEquipment,countServices,countStaff]=await Promise.all([supabase.from("profiles").select("id",{count:"exact",head:true}).eq("role","customer"),supabase.from("equipment").select("id",{count:"exact",head:true}),supabase.from("services").select("id",{count:"exact",head:true}),supabase.from("staff_members").select("id",{count:"exact",head:true})]);const cards=[["Customers",countCustomers.count||0],["Equipment",countEquipment.count||0],["Services",countServices.count||0],["Team members",countStaff.count||0]];return <><PortalHeader eyebrow="Reporting" title="Analytics" description="A live snapshot of Studio Two operations."/><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value])=><article key={label} className="bg-paper p-6"><p className="text-sm text-black/50">{label}</p><p className="mt-8 font-display text-4xl">{value}</p></article>)}</div></>}
