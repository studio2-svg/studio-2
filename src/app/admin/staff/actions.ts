"use server";
import{revalidatePath}from"next/cache";import{z}from"zod";import{requireAdmin}from"@/lib/auth";
const text=(n=500)=>z.string().trim().max(n),optionalId=z.union([z.literal(''),z.uuid()]);function refresh(){revalidatePath('/admin/staff');revalidatePath('/team')}
export async function saveStaffCategory(form:FormData){const v=z.object({id:optionalId,name:text(100).min(1),slug:z.string().regex(/^[a-z0-9-]+$/),description:text(1000),sort_order:z.coerce.number().int(),active:z.string().optional()}).parse(Object.fromEntries(form));const{supabase}=await requireAdmin(),values={name:v.name,slug:v.slug,description:v.description||null,sort_order:v.sort_order,active:v.active==='on'};const{error}=v.id?await supabase.from('staff_categories').update(values).eq('id',v.id):await supabase.from('staff_categories').insert(values);if(error)throw new Error(error.message);refresh()}
export async function saveStaffMember(form:FormData){const v=z.object({id:optionalId,category_id:z.union([z.literal(''),z.uuid()]),name:text(160).min(1),slug:z.string().regex(/^[a-z0-9-]+$/),profile_photo_url:z.union([z.literal(''),z.url()]),bio:text(3000),role_title:text(160),email:z.union([z.literal(''),z.email()]),phone:text(80),base_price:z.coerce.number().min(0),pricing_type:z.enum(['hourly','daily','fixed','per_booking']),status:z.enum(['active','unavailable','leave','archived']),featured:z.string().optional()}).parse(Object.fromEntries(form));const{supabase}=await requireAdmin(),values={category_id:v.category_id||null,name:v.name,slug:v.slug,profile_photo_url:v.profile_photo_url||null,bio:v.bio||null,role_title:v.role_title||null,email:v.email||null,phone:v.phone||null,base_price_minor:Math.round(v.base_price*100),pricing_type:v.pricing_type,status:v.status,featured:v.featured==='on'};const{error}=v.id?await supabase.from('staff_members').update(values).eq('id',v.id):await supabase.from('staff_members').insert(values);if(error)throw new Error(error.message);refresh()}
export async function addPurpose(form:FormData){const v=z.object({name:text(120).min(1),slug:z.string().regex(/^[a-z0-9-]+$/),description:text(1000),sort_order:z.coerce.number().int()}).parse(Object.fromEntries(form));const{supabase}=await requireAdmin();const{error}=await supabase.from('booking_purposes').insert({...v,active:true});if(error)throw new Error(error.message);refresh()}
export async function saveRecommendation(form:FormData){const v=z.object({purpose_id:z.uuid(),staff_category_id:z.uuid(),recommended_quantity:z.coerce.number().int().min(0),minimum_quantity:z.coerce.number().int().min(0),maximum_quantity:z.coerce.number().int().min(0),required:z.string().optional()}).parse(Object.fromEntries(form));if(v.maximum_quantity<v.minimum_quantity||v.recommended_quantity>v.maximum_quantity)throw new Error('Recommendation quantities are inconsistent.');const{supabase}=await requireAdmin();const{error}=await supabase.from('production_requirement_rules').upsert({...v,required:v.required==='on'},{onConflict:'purpose_id,staff_category_id'});if(error)throw new Error(error.message);refresh()}
