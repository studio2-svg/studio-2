import { requireAdmin } from "@/lib/auth";
export default async function AdminPage() { const { profile } = await requireAdmin(); return <><p className="text-xs uppercase tracking-[.22em] text-gold">Operations</p><h1 className="mt-2 font-display text-5xl">Good day, {profile.first_name}.</h1><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Today's bookings","0"],["Revenue today","GH₵0"],["Pending payments","0"],["Needs attention","0"]].map(([k,v]) => <article className="bg-paper p-6" key={k}><p className="text-sm text-black/50">{k}</p><p className="mt-8 font-display text-4xl">{v}</p></article>)}</div></>; }
