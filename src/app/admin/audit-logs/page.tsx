import { requireAdmin } from "@/lib/auth";import {EmptyPanel,PortalHeader} from "@/components/portal-page";
export default async function Page(){const{supabase}=await requireAdmin();const{data}=await supabase.from("audit_logs").select("id,action,entity_type,entity_id,created_at").order("created_at",{ascending:false}).limit(100);return <><PortalHeader eyebrow="Security" title="Audit logs" description="The latest administrative changes across the platform."/>{data?.length?<div className="mt-8 divide-y divide-black/10 bg-paper">{data.map(x=><article key={x.id} className="grid gap-1 p-4 text-sm sm:grid-cols-[10rem_10rem_1fr]"><span>{x.action}</span><span>{x.entity_type}</span><span className="text-black/45">{new Date(x.created_at).toLocaleString()} · {x.entity_id}</span></article>)}</div>:<EmptyPanel title="No activity yet" message="Administrative changes will be recorded here."/>}</>}
