import Link from"next/link";import{requireAdmin}from"@/lib/auth";import{ActionForm}from"@/components/action-form";import{CollectionHeader,SaveButton,TextArea,TextField}from"@/components/admin-form-fields";import{addBlockedPeriod,addPricingRule,addStudio,saveBookingRules,saveHours,saveStudio}from"./actions";
const days=["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"];
export default async function StudioAdmin({searchParams}:PageProps<"/admin/studio">){const{supabase}=await requireAdmin(),query=await searchParams;const{data:studios,error}=await supabase.from("studios").select("*").order("created_at");if(error)throw new Error(error.message);const requested=typeof query.studio==="string"?query.studio:null,studio=studios?.find(x=>x.id===requested)||studios?.[0];return <><CollectionHeader eyebrow="Operations" title="Studios and pricing" description="Each studio has independent details, availability, booking rules, and pricing."/><section className="mt-8 bg-paper p-6"><h2 className="font-display text-2xl">Choose a studio</h2><div className="mt-4 flex flex-wrap gap-2">{studios?.map(item=><Link key={item.id} href={`/admin/studio?studio=${item.id}`} className={`border px-4 py-3 text-sm ${item.id===studio?.id?"border-ink bg-ink text-paper":"border-black/15 bg-white"}`}>{item.name}<small className="ml-2 opacity-60">{item.currency}</small></Link>)}</div><details className="mt-6 border-t border-black/10 pt-5"><summary className="cursor-pointer text-sm font-medium">Add another studio</summary><ActionForm action={addStudio} successMessage="Studio created. Select it above to configure pricing." className="mt-5 grid gap-4 sm:grid-cols-2"><TextField name="name" label="Studio name" required/><TextField name="slug" label="Slug" required/><TextField name="currency" label="Currency" value="GHS" required/><TextField name="timezone" label="Timezone" value="Africa/Accra" required/><div className="sm:col-span-2"><SaveButton label="Create studio"/></div></ActionForm></details></section>{studio?<StudioConfiguration studio={studio} supabase={supabase}/>:<p className="mt-8 bg-paper p-6">Create your first studio to begin.</p>}</>}
async function StudioConfiguration({studio,supabase}:{studio:Record<string,any>;supabase:any}){const[{data:hours},{data:blocks},{data:prices},{data:rules}]=await Promise.all([supabase.from("opening_hours").select("*").eq("studio_id",studio.id).order("day_of_week"),supabase.from("blocked_periods").select("*").eq("studio_id",studio.id).order("starts_at"),supabase.from("pricing_rules").select("*").eq("studio_id",studio.id).order("priority"),supabase.from("booking_rules").select("*").eq("studio_id",studio.id).maybeSingle()]);return <div className="mt-8 grid gap-8"><div className="border-l-4 border-gold bg-ink p-5 text-paper"><p className="text-xs uppercase tracking-[.2em] text-gold">Currently editing</p><h2 className="mt-1 font-display text-3xl">{studio.name}</h2><p className="mt-1 text-sm text-white/55">All rules below apply only to this studio.</p></div><ActionForm action={saveStudio} successMessage={`${studio.name} details saved.`} className="grid gap-4 bg-paper p-6"><h2 className="font-display text-2xl">Studio details</h2><input type="hidden" name="id" value={studio.id}/><div className="grid gap-4 sm:grid-cols-2"><TextField name="name" label="Name" value={studio.name} required/><TextField name="slug" label="Slug" value={studio.slug} required/></div><TextArea name="description" label="Description" value={studio.description}/><TextArea name="address" label="Address" value={studio.address}/><div className="grid gap-4 sm:grid-cols-2"><TextField name="timezone" label="Timezone" value={studio.timezone}/><TextField name="currency" label="Currency" value={studio.currency}/></div><label className="text-sm"><input type="checkbox" name="active" defaultChecked={studio.active} className="mr-2"/>Active</label><SaveButton/></ActionForm><section className="bg-paper p-6"><h2 className="font-display text-2xl">Operating hours · {studio.name}</h2><div className="mt-5 grid gap-3">{days.map((day,index)=>{const item=hours?.find((row:any)=>row.day_of_week===index);return <ActionForm action={saveHours} successMessage={`${day} hours saved for ${studio.name}.`} key={day} className="grid items-end gap-3 border-t border-black/10 pt-3 sm:grid-cols-[1fr_1fr_1fr_auto_auto]"><input type="hidden" name="studio_id" value={studio.id}/><input type="hidden" name="day_of_week" value={index}/><strong className="pb-3 text-sm">{day}</strong><TextField name="opens_at" label="Opens" type="time" value={item?.opens_at?.slice(0,5)||"09:00"}/><TextField name="closes_at" label="Closes" type="time" value={item?.closes_at?.slice(0,5)||"18:00"}/><label className="pb-3 text-sm"><input type="checkbox" name="is_closed" defaultChecked={item?.is_closed} className="mr-2"/>Closed</label><SaveButton/></ActionForm>})}</div></section><ActionForm action={saveBookingRules} successMessage={`Booking rules saved for ${studio.name}.`} className="grid gap-4 bg-paper p-6"><h2 className="font-display text-2xl">Booking rules · {studio.name}</h2><input type="hidden" name="studio_id" value={studio.id}/><div className="grid gap-4 sm:grid-cols-3"><TextField name="minimum_duration_minutes" label="Minimum duration (minutes)" type="number" value={rules?.minimum_duration_minutes||60}/><TextField name="maximum_duration_minutes" label="Maximum duration (minutes)" type="number" value={rules?.maximum_duration_minutes||720}/><TextField name="booking_increment_minutes" label="Increment (minutes)" type="number" value={rules?.booking_increment_minutes||30}/><TextField name="minimum_notice_hours" label="Minimum notice (hours)" type="number" value={rules?.minimum_notice_hours||24}/><TextField name="maximum_advance_days" label="Maximum advance (days)" type="number" value={rules?.maximum_advance_days||365}/><TextField name="hold_duration_minutes" label="Hold duration (minutes)" type="number" value={rules?.hold_duration_minutes||15}/></div><TextArea name="cancellation_policy" label="Cancellation policy" value={rules?.cancellation_policy}/><SaveButton/></ActionForm><div className="grid gap-8 lg:grid-cols-2"><section className="bg-paper p-6"><h2 className="font-display text-2xl">Blocked periods · {studio.name}</h2><ActionForm action={addBlockedPeriod} successMessage={`Period blocked for ${studio.name}.`} className="mt-5 grid gap-4"><input type="hidden" name="studio_id" value={studio.id}/><TextField name="starts_at" label="Starts" type="datetime-local" required/><TextField name="ends_at" label="Ends" type="datetime-local" required/><TextField name="reason" label="Reason" required/><TextArea name="internal_notes" label="Internal notes"/><SaveButton label="Block period"/></ActionForm><div className="mt-6 space-y-2">{blocks?.map((block:any)=><p key={block.id} className="border-t border-black/10 pt-2 text-sm"><strong>{block.reason}</strong><br/><span className="text-black/50">{new Date(block.starts_at).toLocaleString()} – {new Date(block.ends_at).toLocaleString()}</span></p>)}</div></section><section className="bg-paper p-6"><h2 className="font-display text-2xl">Pricing rules · {studio.name}</h2><p className="mt-2 text-sm text-black/50">These prices will never affect another studio.</p><ActionForm action={addPricingRule} successMessage={`Pricing rule added to ${studio.name}.`} className="mt-5 grid gap-4"><input type="hidden" name="studio_id" value={studio.id}/><TextField name="name" label="Rule name" required/><label className="text-sm"><span className="mb-2 block">Type</span><select name="rule_type" className="w-full border border-black/15 bg-white px-3 py-2.5"><option value="hourly">Hourly</option><option value="fixed">Fixed</option><option value="tiered">Tiered</option><option value="percentage">Percentage</option><option value="flat_fee">Flat fee</option></select></label><TextField name="amount" label={`Amount (${studio.currency})`} type="number" value={0}/><TextField name="priority" label="Priority" type="number" value={0}/><label className="text-sm"><input type="checkbox" name="active" defaultChecked className="mr-2"/>Active</label><SaveButton label="Add pricing rule"/></ActionForm><div className="mt-6 space-y-2">{prices?.map((price:any)=><p key={price.id} className="flex justify-between border-t border-black/10 pt-2 text-sm"><span>{price.name}<br/><small className="text-black/45">{price.rule_type}</small></span><strong>{studio.currency} {(price.amount_minor/100).toFixed(2)}</strong></p>)}</div></section></div></div>}
