import{requireAdmin}from"@/lib/auth";import{CollectionHeader}from"@/components/admin-form-fields";import{ServicesManager}from"@/components/services-manager";
export default async function ServicesAdmin(){const{supabase}=await requireAdmin();const{data,error}=await supabase.from("services").select("*").order("sort_order");if(error)throw new Error(error.message);return <><CollectionHeader eyebrow="Operations" title="Services" description="A simple list for viewing, creating, and editing services."/><ServicesManager items={data||[]}/></>}
