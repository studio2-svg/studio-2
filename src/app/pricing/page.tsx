import type{Metadata}from"next";import{PublicNav}from"@/components/public-nav";import{PublicFooter}from"@/components/public-footer";import{getPublishedPage,getPublicData,pageMetadata}from"@/lib/cms";type Rule={id:string;name:string;rule_type:string;amount_minor:number;active:boolean};export async function generateMetadata():Promise<Metadata>{return pageMetadata(await getPublishedPage('pricing'))}
export default async function PricingPage(){const[page,rules]=await Promise.all([getPublishedPage('pricing'),getPublicData<Rule>('pricing_rules')]);return <main className="min-h-screen bg-paper"><PublicNav/><section className="mx-auto max-w-6xl px-6 py-20"><p className="text-xs uppercase tracking-[.28em] text-gold">{page?.subtitle}</p><h1 className="mt-4 font-display text-7xl">{page?.title}</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-black/55">{page?.description}</p><div className="mt-14 divide-y divide-black/15">{rules.length?rules.map(rule=><article key={rule.id} className="flex items-end justify-between py-7"><div><p className="text-xs uppercase tracking-[.18em] text-gold">{rule.rule_type.replace('_',' ')}</p><h2 className="mt-2 font-display text-3xl">{rule.name}</h2></div><p className="font-display text-3xl">GHS {(rule.amount_minor/100).toFixed(2)}</p></article>):<p className="py-8 text-black/50">Pricing will appear here when configured by Studio Two.</p>}</div></section><PublicFooter/></main>}
